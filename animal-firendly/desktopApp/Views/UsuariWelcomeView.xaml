<UserControl x:Class="DesktopApp.Views.UsuariWelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontSize="24">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding MissatgeBenvinguda}" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="50"/>
        <Label Grid.Row="1" Content="{Binding MissatgeDefinicioUsuari}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        
    </Grid>
</UserControl>
